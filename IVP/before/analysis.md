# Capture-The-Flag — "Before" Cohesion, Coupling & Scatter Analysis

> Complement to `metrics.md`. Measures the committed "before" modularisation at every granularity against the 29-driver change-driver model (`CD-01..CD-30`, CD-29 retired). Terminology and formulas follow the IVP paper series (`cohesion-2-nature`, `cohesion-4-causal`):
> - **causal cohesion** `H_causal(M) = (purity(M), completeness(M))`
> - `purity(M) = 1 / |𝒜(M)|` where `𝒜(M)` = the set of *distinct change-driver sets* among M's elements (type-level or member-level, per the granularity).
> - `completeness(M) = min over A ∈ 𝒜(M) of |M ∩ [A]| / |[A]|`, where `[A]` = all elements system-wide with driver-set A.
> - **HHI contamination extent** = `1 − Σᵢ sᵢ²` where `sᵢ` is the share of each driver-set within the module (supplements purity; = 0 when pure, → 1 as contamination spreads evenly).

Scope: 293 annotated types, 57 namespaces, 43 change drivers (CD-02..CD-28, CD-30 plus platform sub-drivers CD-31..CD-44; CD-01 decomposed, 0 citations). Class-level cohesion is computed over 752 attributed members (member-level; see caveat 2 — the member count is approximate). All figures are the canonical recomputation on the corrected annotation layer (CD-29 retired, tests re-rooted on code-under-test domain drivers, exhaustive annotation review), generated by `IVP/tools/IvpMeasure.java` + `IVP/tools/ClassCohesion.java`.

Note: Composite types (like class, record, struct, interface) are modules exactly like namespaces.
---

## A. Causal cohesion

### A.1 — Class-level (members are the module's elements)

Lowest-purity classes (most contaminated; purity = 1 / #distinct member driver-sets):

| class | members | purity | completeness | contamination extent (1−HHI) |
|---|---|---|---|---|
| PlayerInfo | 52 | 0.091 | 0.130 | 0.811 |
| Team | 18 | 0.143 | 0.022 | 0.710 |
| GunGameSystem | 10 | 0.200 | 0.019 | 0.720 |
| WeaponSelectionSystem | 7 | 0.200 | 0.022 | 0.776 |
| ClassSelectionSystem | 6 | 0.250 | 0.136 | 0.667 |
| Flag | 11 | 0.250 | 0.022 | 0.446 |
| FlagSystem | 5 | 0.250 | 0.024 | 0.720 |
| MapRotationService | 9 | 0.250 | 0.100 | 0.691 |
| MapRotationSystem | 8 | 0.250 | 0.300 | 0.688 |
| PlayerRepository | 38 | 0.250 | 1.000 | 0.594 |

**Reading.** `PlayerInfo` is the most impure class: 52 attributed members realise ~11 distinct driver sets (account identity, stats, coins, rank, roles, skin, team, persistence columns …). Its completeness 0.13 means its member driver-sets are themselves scattered — each of its driver sets is shared with other classes, so `PlayerInfo` holds only a fraction of each set system-wide. This is the signature of a *god aggregate*: the account entity absorbs many domain concerns instead of delegating them. `PlayerRepository` (purity 0.250, completeness 1.0) is the contrast case: it also mixes driver sets, but it *owns* each of them entirely (all elements of each set live here) — its impurity is a repo-contract composite, decreed by the persistence axis.

### A.2 — Namespace-level (types are the module's elements)

The namespace is the analysis's **primary module granularity** — it is where the "57 modules" figure lives. Full per-namespace table, sorted ascending by purity (most contaminated first). `purity = 1 / |distinct driver-sets|`; `completeness = min over sets A of |M ∩ [A]| / |[A]|`.

| namespace | classes | tokens | sets | purity | completeness |
|---|---|---|---|---|---|
| CTF.Application.Players | 10 | 12 | 10 | 0.100 | 0.143 |
| CTF.Application.Players.Accounts.Statistics | 10 | 15 | 10 | 0.100 | 1.000 |
| CTF.Application.Teams | 13 | 16 | 10 | 0.100 | 0.333 |
| CTF.Application.Teams.Flags | 8 | 12 | 8 | 0.125 | 0.042 |
| CTF.Application.Players.Accounts.Roles | 9 | 10 | 7 | 0.143 | 1.000 |
| CTF.Application.Players.GeneralCommands | 7 | 8 | 7 | 0.143 | 0.143 |
| CTF.Application.GunGames | 12 | 13 | 6 | 0.167 | 0.143 |
| CTF.Application.Players.Weapons | 6 | 11 | 6 | 0.167 | 0.091 |
| CTF.Application.Teams.ClassSelection | 6 | 10 | 6 | 0.167 | 0.333 |
| CTF.Application.Teams.Flags.Events | 6 | 10 | 6 | 0.167 | 1.000 |
| CTF.Application.Tests.Players.Accounts | 9 | 8 | 6 | 0.167 | 0.286 |
| CTF.Application.Maps | 14 | 9 | 5 | 0.200 | 0.875 |
| CTF.Application.Players.Chats | 5 | 8 | 5 | 0.200 | 1.000 |
| CTF.Host.Extensions | 5 | 6 | 5 | 0.200 | 1.000 |
| Persistence.InMemory | 6 | 5 | 5 | 0.200 | 0.667 |
| Persistence.MariaDB | 5 | 6 | 5 | 0.200 | 0.143 |
| Persistence.SQLite | 5 | 7 | 5 | 0.200 | 0.143 |
| Persistence.Tests.Common | 6 | 9 | 5 | 0.200 | 0.500 |
| CTF.Application.GunGames.Results | 6 | 8 | 4 | 0.250 | 0.042 |
| CTF.Application.Players.Accounts.Authentication | 5 | 7 | 4 | 0.250 | 1.000 |
| CTF.Application.Players.AntiCBug | 4 | 9 | 4 | 0.250 | 1.000 |
| CTF.Application.Players.Combos | 5 | 12 | 4 | 0.250 | 0.500 |
| CTF.Application.Players.TopPlayers | 6 | 7 | 4 | 0.250 | 0.333 |
| CTF.Application.Players.Vitalities | 6 | 8 | 4 | 0.250 | 0.500 |
| CTF.Application.Teams.Flags.Carriers | 4 | 9 | 4 | 0.250 | 0.500 |
| CTF.Application.Tests.Players.Weapons | 5 | 5 | 4 | 0.250 | 0.091 |
| CTF.Host.Services | 4 | 4 | 4 | 0.250 | 0.500 |
| CTF.Application.Players.Accounts.Profile | 3 | 8 | 3 | 0.333 | 1.000 |
| CTF.Application.Teams.Matches | 3 | 4 | 3 | 0.333 | 0.200 |
| CTF.Application.Teams.Statistics | 3 | 7 | 3 | 0.333 | 1.000 |
| CTF.Application.Tests.Fakes | 5 | 5 | 3 | 0.333 | 0.125 |
| CTF.Host | 3 | 8 | 3 | 0.333 | 1.000 |
| Persistence.Tests.Common.DatabaseProviders | 3 | 6 | 3 | 0.333 | 1.000 |
| CTF.Application.Maps.Rotation | 2 | 12 | 2 | 0.500 | 1.000 |
| CTF.Application.Players.Accounts | 2 | 5 | 2 | 0.500 | 1.000 |
| CTF.Application.Players.Chats.Definitions | 4 | 4 | 2 | 0.500 | 1.000 |
| CTF.Application.Players.Combos.Definitions | 6 | 3 | 2 | 0.500 | 0.500 |
| CTF.Application.Players.Headshots | 2 | 8 | 2 | 0.500 | 1.000 |
| CTF.Application.Players.Pause | 3 | 4 | 2 | 0.500 | 0.667 |
| CTF.Application.Players.Ranks | 5 | 4 | 2 | 0.500 | 0.667 |
| CTF.Application.Players.Weapons.Catalogs | 10 | 2 | 2 | 0.500 | 0.500 |
| CTF.Application.Teams.Flags.AutoReturn | 2 | 5 | 2 | 0.500 | 0.500 |
| CTF.Application.Tests.GunGames | 8 | 3 | 2 | 0.500 | 0.083 |
| CTF.Application.Tests.Maps | 6 | 4 | 2 | 0.500 | 1.000 |
| CTF.Application.Tests.Teams | 5 | 4 | 2 | 0.500 | 0.571 |
| CTF.Application | 1 | 1 | 1 | 1.000 | 0.143 |
| CTF.Application.GunGames.WeaponProgressions | 5 | 1 | 1 | 1.000 | 0.208 |
| CTF.Application.GunGames.WeaponProgressions.Definitions | 8 | 1 | 1 | 1.000 | 0.333 |
| CTF.Application.Tests | 1 | 4 | 1 | 1.000 | 0.500 |
| CTF.Application.Tests.Players.Extensions | 1 | 4 | 1 | 1.000 | 1.000 |
| CTF.Application.Tests.Players.Ranks | 3 | 3 | 1 | 1.000 | 0.600 |
| CTF.Application.Tests.Players.TopPlayers | 1 | 4 | 1 | 1.000 | 1.000 |
| CTF.Application.Tests.Players.Vitalities | 2 | 3 | 1 | 1.000 | 0.667 |
| CTF.Application.Tests.Teams.Flags | 1 | 3 | 1 | 1.000 | 0.143 |
| Persistence.SQLite.Extensions | 2 | 1 | 1 | 1.000 | 1.000 |
| Persistence.Tests.Players | 5 | 4 | 1 | 1.000 | 1.000 |
| SampSharp | 1 | 2 | 1 | 1.000 | 1.000 |

**Reading.** Mean purity 0.442, min purity 0.100, across 57 namespaces. 45 of 57 are composite (purity < 1). The impurity concentrates at the top: `Players`, `Accounts.Statistics` and `Teams` (one distinct set per class, purity 0.100) are maximally contaminated; `Teams.Flags` (8 sets) follows. The low-completeness namespaces (`Teams.Flags` 0.042, `GunGames.Results` 0.042, `Tests.GunGames` 0.083, `Players.Weapons` 0.091) hold only a *fraction* of each of their driver sets system-wide → their driver sets are also scattered elsewhere (the scatter defect). The pure tail (purity 1.0) is dominated by single-driver domains (`WeaponProgressions`, `Ranks`) and small test/tooling namespaces.

### A.3 — Assembly-level (project/assembly = module, types as elements)

| assembly | classes | distinct type-driver-sets | purity |
|---|---|---|---|
| src/Application | 201 | 122 | 0.008 |
| src/Host | 13 | 13 | 0.077 |
| Persistence.InMemory | 6 | 5 | 5 |
| Persistence.MariaDB | 5 | 6 | 5 |
| Persistence.SQLite | 5 | 7 | 5 |
| tests/Application.Tests | 47 | 21 | 0.048 |
| tests/Persistence.Tests | 14 | 9 | 0.111 |

**Reading.** At assembly granularity purity collapses toward 0 (0.008 for the Application core, which hosts 122 distinct driver-sets) because a whole assembly legitimately bundles many driver-equivalence classes. This is *not* a defect at assembly granularity — it is the expected result of an assembly being a deployment unit, not a change-coupling unit. Cohesion is granularity-relative; the assembly number is a floor, not a target. The meaningful granularities are class, and especially namespace.

## B. Coupling (change-coupling, not dependency)

### B.1 — Inter-namespace shared-driver overlap

Top pairs of namespaces sharing the most change drivers (Jaccard on driver sets):

| namespace A | namespace B | shared drivers | jaccard |
|---|---|---|---|
| CTF.Application | 1 | 1 | 1 |
| CTF.Application.GunGames | 12 | 13 | 6 |
| CTF.Application.GunGames.Results | 6 | 8 | 4 |
| CTF.Application.GunGames.WeaponProgressions | 5 | 1 | 1 |
| CTF.Application.GunGames.WeaponProgressions.Definitions | 8 | 1 | 1 |
| CTF.Application.Maps | 14 | 9 | 5 |
| CTF.Application.Maps.Rotation | 2 | 12 | 2 |
| CTF.Application.Players | 10 | 12 | 10 |
| CTF.Application.Players.Accounts | 2 | 5 | 2 |
| CTF.Application.Players.Accounts.Authentication | 5 | 7 | 4 |
| CTF.Application.Players.Accounts.Profile | 3 | 8 | 3 |
| CTF.Application.Players.Accounts.Roles | 9 | 10 | 7 |

**Reading.** The strong overlaps fall into two kinds: (a) **parent–child** namespace pairs (Teams↔Teams.Flags, Players↔Teams.ClassSelection, Accounts↔Accounts.Statistics, Host↔Host.Extensions) — expected containment; and (b) **cross-cutting-driver clusters** — GunGames shares drivers with Statistics/Combos/Weapons because the GunGame *gate* (CD-07) legitimately suspends/replaces those subsystems' behaviours (a game-designed composite, not scatter). The persistence providers share their core repo drivers (CD-17/18/20/21 + dialect) by the repository-contract axis — decreed, not spurious.

### B.2 — Per-driver causal reach (which namespaces each driver touches)

| driver | namespaces touched |
|---|---|
| CD-17 | 26 |
| CD-31 | 23 |
| CD-36 | 21 |
| CD-32 | 20 |
| CD-15 | 18 |
| CD-09 | 16 |
| CD-10 | 16 |
| CD-20 | 15 |
| CD-02 | 14 |
| CD-43 | 14 |
| CD-26 | 13 |
| CD-27 | 13 |
| CD-33 | 13 |
| CD-21 | 11 |
| CD-03 | 9 |
| CD-07 | 9 |
| CD-11 | 9 |
| CD-08 | 8 |
| CD-25 | 8 |
| CD-34 | 8 |
| CD-35 | 8 |
| CD-06 | 6 |
| CD-12 | 6 |
| CD-18 | 6 |
| CD-37 | 6 |
| CD-40 | 6 |
| CD-22 | 5 |
| CD-38 | 5 |
| CD-41 | 5 |
| CD-44 | 5 |
| CD-19 | 4 |
| CD-24 | 4 |
| CD-30 | 4 |
| CD-42 | 4 |
| CD-04 | 3 |
| CD-23 | 3 |
| CD-05 | 2 |
| CD-13 | 2 |
| CD-14 | 1 |
| CD-16 | 1 |
| CD-28 | 1 |

The long tail is the decreed horizontal axes (platform sub-drivers/config/commands/repo/test-tooling); CD-01 and CD-39 activate in 0 and 0 type-set containers here respectively (CD-01 decomposed with no residual citations; CD-39's single annotated element exists only in the refactored layout). The *change-coupling* candidates for the IVP regroup are the mid-band game-domain drivers (CD-02, CD-06, CD-07, CD-10, CD-15) whose reach is dispersion, not platform necessity. With CD-29 retired, the test namespaces extend the *domain* drivers' reach — test↔production alignment is measured in §F instead.

---

## C. Scatter & contamination (the two IVP defect types)

- **Scatter** — `23` distinct type-driver-sets are split across **more than one** namespace (their equivalence class `[A]` is not contained in a single module).
- **Contamination** — `45 of 57` namespaces co-locate types with **differing** driver sets.

### C.1 — Essential vs spurious coupling (rough classification)

Using the `constraints.md` axes, a co-location is **essential** when a platform/persistence/composition/test axis decrees it; otherwise **spurious** (change-coupling, the IVP objective's target).

- **Spurious (removable by IVP regroup):** all `CTF.Application.*` production namespaces — `Players` (10 sets), `Accounts.Statistics` (10), `Teams` (10), `Teams.Flags` (8), `GeneralCommands` (7), `Roles` (7), `ClassSelection` (6), `Weapons` (6), `GunGames` (6), etc. These mix differing driver sets by *topic*, not by a decreed axis.
- **Essential (irreducible):** `Persistence.InMemory/MariaDB/SQLite` (repo-contract axis), `CTF.Host`/`Host.Extensions`/`Host.Services` (composition/platform axis), and the *test* namespaces (code-under-test domain-driver axis).

> The essential co-locations are enumerated per element in `IVP/constraints.md` **Appendix A** (composition root, three providers, repository ports, `IGunGameMode` gate, flag-event handlers, `*.Designer.cs`, fakes, provider dispatch). Anything not listed there is **spurious**.

---

## D. Change-propagation cost

`κ(γ)` = modules (namespaces) touched per driver activation. Full table in `metrics.md` §1.

- Highest `κ`: CD-17 config (26), CD-31 player entity (23), CD-36 client-message (21), CD-32 ECS (20), CD-15 command set (18), CD-09 authorization (16), CD-10 stats (16), CD-20 repo (15). CD-01 = 0 (decomposed).
- These are decreed horizontal axes. The IVP objective (minimise change-propagation) can only compress the **spurious** portion of the mid-band (CD-02 game-rules 14, CD-07 GunGame 9, CD-06 coins 6, CD-12 rotation 6).

> **Unmeasurable without an artifact:** activation-frequency weighting `λ(γ)`. No release-cadence / beta-frequency artifact exists in the repo (`.env`, config, docs, git history are the only sources, and none record driver activation rates). Weighted cost `Σ λ(γ)·κ(γ)` is therefore **not computed**; it is flagged for a future pass once a `λ` source is declared. This is not fabricated.

---

## F. Test ↔ production driver alignment

With CD-29 retired, a test's real change drivers are the **domain drivers of the code under test** (plus the framework drivers). Alignment check — does a test/fake carry the domain driver of the production element it asserts or mimics?

| test class | domain driver (code under test) | production mirror |
|---|---|---|
| FakePlayer/FakeCarrier/FakePlayer2/FakePlayer3 | CD-31 | `Player` (platform surface) ✓ |
| ProcessKillTests / WeaponLevelTests / … | CD-07 | GunGame progression ✓ |
| IsCarryingEnemyFlagTests | CD-02 | Team/Flag (PlayerInfo.IsCarryingEnemyFlag) ✓ |
| PasswordTests | CD-08 | PlayerInfo.SetPassword ✓ |
| WeaponCatalogTests / Settings / Type | CD-04 | WeaponCatalog ✓ |

The domain-driver alignment holds on the sample: each test/fake carries the code-under-test's domain driver, not a generic seam abstraction. A full machine check requires an explicit test-namespace → production-namespace mirror map (the sample above is manual; the mirror is derivable by stripping `.Tests` and `Persistence.Tests` → `Persistence`).

---

## Bottom line

The "before" state is a **low-purity, topic-grouped modularisation**: 293 classes (elements) realize **171 distinct change-driver sets** but are placed in only **57 namespaces** (the theory's modules); 45/57 namespaces are composite and 23 of the 171 driver-sets are scattered across >1 namespace. Mean namespace purity 0.442, assembly purity near 0 (expected), class-level purity as low as 0.083 (`PlayerInfo`). The 57-vs-171 gap is the IVP non-conformance (the code fuses 171 `E/Γ` classes into 57 topic modules); the change-coupling drivers (game-rules, stats, coins, GunGame, command-set) are dispersed across namespaces beyond what the platform/persistence/composition axes decree — this dispersion is the exact quantity an IVP regroup (group by identical driver set, subject to `constraints.md`) would tighten, and it is what the "after" measurement (in the `apply-ivp` worktree) must show reduced.

---

## Caveats & limitations (honest)

1. **Purity is over *driver sets*, not driver count.** `purity(M) = 1 / |distinct driver sets|`. A class whose members all share `{CD-02, CD-10}` is pure; "multi-driver" ≠ impure. Only differing *sets* co-located make a module impure. "Drivers per class/namespace" figures are token counts, a proxy for — not equal to — the set-based contamination.
2. **Member-level detection is a heuristic.** Members were found by indentation + visibility keyword + the member's own nearby `Change drivers` remark (that line only). A small number mis-bind: `PlayerInfo` has 52 attributed members while a few top-of-class `const`/`partial Regex` declarations are unannotated and skipped. Class *rankings* are robust; exact member counts are ± a few.
3. **Essential-vs-spurious is grounded in `constraints.md` Appendix A**, which cites each essential composite element (`Startup`, the three providers, the `IPlayerRepository` ports, `IGunGameMode`, `*.Designer.cs`, the fakes, the provider dispatch) to its decreed axis. The §C.1 "spurious" bucket (topic-grouped `CTF.Application.*` namespaces) is everything *not* in that appendix. For any co-location not listed here nor in Appendix A, the default classification is **spurious** (change-coupling scatter).
4. **λ(γ) unmeasured.** Weighted cost `Σ λ(γ)·κ(γ)` needs a per-driver activation-frequency artifact, which the repo does not contain. Only `κ(γ)` (module-touch count) is reported.
5. **Test-namespace completeness is inflated** (singleton driver sets) and **assembly purity is near 0** (expected, not a defect) — both granularity artifacts documented in `metrics.md` §7.